/**
 * NEXORA MD - API Configuration
 * API endpoints and keys ONLY. Branding lives in settings.js
 */

require('dotenv').config();

global.APIs = {
  // Filled in when downloader/media plugins are added
};

global.APIKeys = {
  // Filled in when downloader/media plugins are added
};

module.exports = {
  WARN_COUNT: 3
};